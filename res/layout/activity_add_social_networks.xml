<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#33b5e5"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="10dp"
    tools:context=".AddSocialNetworksActivity" >

    <TextView
        android:id="@+id/me"
        style="@style/button_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:layout_marginLeft="106dp"
        android:layout_marginTop="28dp"
        android:text="Let&apos;s get you started with a smarter way to share"
        android:textSize="18dp" />

    <ListView
        android:id="@+id/socialnetworkslistView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/me"
        android:layout_below="@+id/me"
        android:layout_margin="10dp"
        android:background="@drawable/list_gradient"
		android:scrollingCache="false"
		android:divider="#e4e4e4"
		android:dividerHeight="2px" >
    </ListView>
    
    <Button
         android:id="@+id/recordpbutton"
         android:layout_width="fill_parent"
         android:layout_height="wrap_content"
         android:layout_alignLeft="@+id/socialnetworkslistView"
         android:layout_below="@+id/socialnetworkslistView"
         android:layout_marginTop="15dp"
         android:text="Get Started"
         android:background="@drawable/red_button"
         style="@style/button_text"
         android:onClick="start" />

</RelativeLayout>